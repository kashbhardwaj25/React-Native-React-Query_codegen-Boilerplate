import { SafeAreaView } from 'react-native'

import FeedCard from '../../../components/FeedCard'

const Home = () => {
  return (
    <SafeAreaView>
      <FeedCard />
    </SafeAreaView>
  )
}

export default Home
