import { SafeAreaView } from 'react-native'

import AppText from '../../../components/shared/AppText'

const Profile = () => {
  return (
    <SafeAreaView>
      <AppText>This is the User's Profile</AppText>
    </SafeAreaView>
  )
}

export default Profile
